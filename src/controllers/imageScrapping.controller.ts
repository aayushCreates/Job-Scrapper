import { Request, Response, NextFunction } from "express";


export const scrapeImage = async (req: Request, res: Response, next: NextFunction)=> {
    try {
        
    } catch(err) {
        console.log("Error in image scrapping", err);
    }
}
